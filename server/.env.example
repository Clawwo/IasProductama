# Database
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/jogjadrumband?schema=public"

# JWT access tokens
JWT_ACCESS_SECRET="change-me-access-secret"
# Optional: fallback secret if JWT_ACCESS_SECRET not set
JWT_SECRET="change-me-default-secret"
# Access token lifetime (e.g., 15m, 1h)
JWT_ACCESS_EXPIRES="15m"

# Sessions / refresh tokens
# Session (refresh token) lifetime in milliseconds. Default is 7 days.
SESSION_TTL_MS=604800000

# App
PORT=3000
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173,https://iasproductama.site,https://api.iasproductama.site"
